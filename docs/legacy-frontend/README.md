These files are kept for historical reference only and are not used by the application anymore.

- app-old.js: the original monolithic JS frontend.
- app-new.js: an early modular rewrite prototype before the TypeScript migration.

The frontend is now authored in TypeScript under backend/public/ts and compiled to backend/public/js during builds.
