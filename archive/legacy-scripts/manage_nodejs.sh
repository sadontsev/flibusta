#!/bin/bash
# Archived legacy wrapper. Prefer running Node manage CLI directly:
#   npm --prefix backend run manage <command>
# or via Admin API.

# Original content retained for reference below.

set -e
# ... original manage_nodejs.sh content was here (see repo history)
