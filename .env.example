# Backend app secrets
SESSION_SECRET=please-change-me
JWT_SECRET=please-change-me

# Optional: SMB/NAS host path for books.
# If set, docker-compose.yml will mount this path into /app/flibusta.
# Example for macOS where the SMB share is mounted under /Volumes:
# BOOKS_HOST_PATH=/Volumes/NAS/flibusta
# If not set, it falls back to ./flibusta in the repo.
#BOOKS_HOST_PATH=
